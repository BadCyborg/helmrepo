apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: {{ .Values.cloneservice.port }}
      targetPort: {{ .Values.cloneservice.targetPort }}
  type: {{ .Values.cloneservice.type }}
